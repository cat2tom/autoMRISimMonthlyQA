testConfigFile='examplemMO.ini';

% [imDir,tmpTxtFile,recordTxtFile,headCell,matFile] = readConfigFileMO(testConfigFile )

dirs = readConfigFileMO(testConfigFile );

dirs{1}

dirs{2}

dirs{3}

dirs{4}

dirs{5}

